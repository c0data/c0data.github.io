# c0data.github.io

The landing page for [C0DATA](https://github.com/c0data) — structured data built
on ASCII C0 control codes. Served at <https://c0data.github.io>.

`index.html` is a single self-contained page (inline CSS/JS). Extracted from the
original Crystal repo so the site is independent of any one implementation.
